name = "task-executor"
description = "Executes one approved work item and creates the feature PR early when authorized; MANUAL stops after implementation and verification, while an active AUTO run handles Reviewer rework until Root closure."

model = "gpt-5.6-luna"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Read and follow .agents/skills/task-executor/SKILL.md for this role's invocation, responsibilities, and authorization boundaries.
"""
